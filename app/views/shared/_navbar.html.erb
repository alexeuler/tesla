<div class="zen-navbar">
  <span class="zen-navbar-left">
    <%=image_tag 'logo.png'%>
    <span class="zen-navbar-header">
      <p class="zen-navbar-brand">Тесла</p>
      <p class="zen-navbar-brand-desc">Лучшие зарубежные новинки</p>
    </span>
  </span>
  <div class="zen-navbar-right">
    <table>
      <tr>
	<td class="<%=controller.controller_name=='posts' ? "active" : ""%>"><%= link_to 'Блог', blog_posts_path %></td>
	<td class="<%=controller.controller_name=='items' ? "active" : ""%>"><%= link_to 'Магазин', store_items_path %></td>
	<td class="<%=['sessions', 'registrations', 'passwords'].include?(controller.controller_name) ? "active" : ""%>"><%= user_signed_in? ? link_to('Выйти', destroy_user_session_path, method: :delete) : link_to('Войти', new_user_session_path) %></td>
      </tr>
    </table>
  </div>
</div>
